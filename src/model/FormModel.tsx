export interface AttributeClassTagInput {
    className: string;
    id: string;
    type: string;
    placeholder:string;
}
export interface TextLabel {
    htmlFor: string;
    text: string;
    className: string;
}
